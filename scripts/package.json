{
  "name": "bible-data-scripts",
  "version": "1.0.0",
  "description": "Scripts to generate SQLite DB and semantic embeddings for the Bible app",
  "type": "module",
  "scripts": {
    "generate:db": "node generate_db.js",
    "generate:embeddings": "node generate_embeddings.js",
    "generate:all": "node generate_db.js && node generate_embeddings.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^11.9.1"
  }
}
